import React from 'react';
import { NavLink } from 'react-router-dom';

const detalleItem = (props) => (
    <div>
       <p>Acá tenes el tìtulo de la nota={props.title}</p>
       <h4>Nachito chinwenwencha</h4>
    </div>
);

export default detalleItem; 